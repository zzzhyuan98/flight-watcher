# Memory Log — 2026-03-24

## User Instructions & Decisions (SerpAPI / Flight Watcher)

- User asked to **find/check SerpAPI key** status in the project.
- Confirmed: project had **no working `SERPAPI_API_KEY`** initially (only README placeholder).
- User requested where to configure key.
- Instruction given:
  - Create `.env` at project root: `C:\Users\PC123\.openclaw\workspace\.env`
  - Add: `SERPAPI_API_KEY=<real_key>`
- `.env` file was created as template, then user updated with real key.
- Verified key is set (non-placeholder) and SerpAPI account endpoint is reachable.

## Safety / Secret-Handling Rule

- **Do not store or echo the raw SerpAPI key in memory files, chat summaries, commits, or docs.**
- Only store:
  - whether key exists,
  - where it is configured,
  - validation result (working/not working).

## Cron Job Update (User-approved)

- Existing cron job updated from fixed dates to flexible date combinations for Songkran check:
  - Depart: `2027-04-10` or `2027-04-11`
  - Return: `2027-04-15` or `2027-04-16`
- Cron prompt now runs all 4 combinations and selects the cheapest result.

## Command Notes

- `npm run compare-origins -- --origins ...` had argument parsing issue in this environment.
- Working command format used:
  - `npx tsx src/cli.ts compare-origins --origins "JHB,KUL" --destination BKK --depart-date <date> --return-date <date> --adults 2 --cabin economy --baggage carry_on --currency MYR`

## Current Preference from User

- User wants flight checks with flexible date windows and best-price output.
